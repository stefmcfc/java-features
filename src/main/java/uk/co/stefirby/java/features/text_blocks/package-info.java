/**
 * Text blocks (15): multi-line string literals ({@code """}) that avoid
 * escape-heavy concatenation for embedded JSON, SQL, or other structured
 * text used across the example set. Also hosts the stage-10/12
 * text-adjacent API examples — the Java 11 {@code String} additions
 * ({@code isBlank()}, {@code strip()}, {@code repeat()}, {@code lines()}),
 * {@code Files.writeString()}/{@code readString()} (11), and the Java
 * 12/15 {@code String.transform()}/{@code indent()}/{@code formatted()}
 * trio — as the spec's package layout defines no dedicated package for
 * them.
 */
package uk.co.stefirby.java.features.text_blocks;
