/**
 * The RandomGenerator interface and its factories (17), used here to drive
 * a seeded Premier League matchweek simulator — Math.clamp() (21) keeps
 * every simulated scoreline inside a plausible range.
 */
package uk.co.stefirby.java.features.random;
