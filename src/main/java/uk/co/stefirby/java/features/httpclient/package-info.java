/**
 * {@link java.net.http.HttpClient} (11): the built-in HTTP/2-capable client
 * that replaced {@code HttpURLConnection} for outbound calls, contrasted
 * with Spring Framework 7's client-side options — the fluent
 * {@code RestClient} and a declarative {@code @HttpExchange} interface —
 * all three fetching the same stage-4 payload.
 */
package uk.co.stefirby.java.features.httpclient;
