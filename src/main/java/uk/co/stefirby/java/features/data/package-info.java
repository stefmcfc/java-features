/**
 * The shared in-memory Premier League dataset ({@code Player}, {@code Team},
 * {@code Match}, {@code PremierLeagueDataBase}) that every other topic
 * package queries, so the same feature can be demonstrated against
 * realistic, relatable data instead of throwaway sample values. No
 * persistence layer — see {@code specs/modern-java-features-spec.md}.
 */
package uk.co.stefirby.java.features.data;
