/**
 * Local variable type inference with {@code var} (10): reducing boilerplate
 * on local declarations where the type is already obvious from the
 * initializer, without making the language dynamically typed.
 */
package uk.co.stefirby.java.features.var;
