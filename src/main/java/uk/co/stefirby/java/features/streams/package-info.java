/**
 * Stream API additions introduced after Java 8: {@code Stream.toList()} (16),
 * {@code Stream.mapMulti()} (16), {@code Collectors.teeing()} (12),
 * {@code takeWhile()}/{@code dropWhile()} (9), the predicate-based
 * {@code Stream.iterate()} overload (9), {@code Stream.ofNullable()} (9), and
 * Sequenced Collection stream views (21). Each example queries the shared
 * Premier League dataset in {@code uk.co.stefirby.java.features.data} so the
 * same query can be compared across JDK versions.
 */
package uk.co.stefirby.java.features.streams;
